@page "/studies"
@attribute [Authorize(Roles = "Administrator")]

<div class="container pt-4">
    <div class="card shadow-lg p-4 mb-5" style="border-radius: 12px;">
        <h3 class="mb-4">Studies</h3>

        <!-- Botón + Filtro -->
        <div class="row align-items-center mb-3">
            <div class="col-md-3 d-flex justify-content-start">
                <button class="btn btn-gradient-primary d-flex align-items-center gap-2" style="width:130px" @onclick="() => ShowModalAsync()">
                    <i class="fa fa-plus-circle"></i> New
                </button>
            </div>
            <div class="col-md-9 d-flex justify-content-end">
                <FilterComponent ApplyFilter="SetFilterValue" />
            </div>
        </div>

        <hr />
        <GenericList MyList="Studies">
            <Body>
                <div class="table-responsive">
                    <table class="table table-hover table-striped align-middle">
                        <thead class="table-dark">
                            <tr>
                                <th>Study</th>
                                <th>Edoc Cat</th>
                                <th>Active</th>
                                <th></th>
                                <th></th>
                                <th colspan="2">Study</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Studies!)
                            {
                                <tr>
                                    <td>@item.FullStudy</td>
                                    <td>@item.TotalEdocCategories</td>
                                    <td>
                                        <input type="checkbox" class="form-check-input" checked="@item.Active" disabled />
                                    </td>
                                    <td style="width:1px;">
                                        <button title="Edoc Category" class="btn-shadow-black" @onclick="() => ShowModalEdocAsync(item.StudyId)">
                                            <i class="fa fa-file-text"></i>
                                        </button>
                                    </td>                              
                                    <td style="width:1px;">
                                        <span class="mx-1">|</span>
                                    </td>

                                    <td style="width:1px;">
                                        <button title="Edit Study" class="btn-shadow-black" @onclick="() => ShowModalAsync(item.StudyId, true)">
                                            <i class="fa fa-edit"></i>
                                        </button>
                                    </td>
                                    <td style="width:1px;">
                                        <button title="Delete Study" class="btn-shadow-danger" @onclick="() => DeleteAsync(item.StudyId)">
                                            <i class="fa fa-trash"></i>
                                        </button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </Body>
        </GenericList>
        <!-- Tabla -->
        <hr />
        @if (Studies is not null)
        {
            <!-- Paginación -->
            <Pagination CurrentPage="CurrentPage"
                        SelectedPage="SelectedPage"
                        TotalPages="TotalPages" />
        }
    </div>
</div>
